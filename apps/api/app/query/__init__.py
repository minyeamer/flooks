"""Query bootstrap package for FLooks.

The package holds the in-memory dataset manifest registry and the semantic
validator used by the first governed query API surfaces.
"""

from __future__ import annotations

__all__ = ["registry", "validator"]